####################################
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        encrypt.enabled: false
        git:
          uri: https://github.com/pixipanda/spring-config-repo.git
          searchPaths: ordersservice,paymentsservice